---
- hosts: all
  roles:
    - tizen
    #- common
    #- { role: dotfiles }
    ##- owncloud

#- hosts: workstations
  #roles:
    #- ssh
    #- R
    #- vscode
    #- z
    #- node
    #- { role: lutris, ignore_errors: yes, when: ansible_distribution != "MacOSX" }
    #- { role: virtualbox, ignore_errors: yes }
    ## - kvm
    #- { role: vagrant, ignore_errors: yes, when: ansible_distribution != "MacOSX" }
    #- { role: geerlingguy.docker, become: true, vars: {docker_users: ['{{ansible_user_id}}']} }
    #- { role: rvm.ruby, ignore_errors: yes, when: ansible_distribution != "MacOSX" }
    ## - java
    #- { role: debian, when: ansible_os_family == "Debian" }
    #- { role: osx, when: ansible_distribution == "MacOSX" }
    #- { role: x11, when: ansible_distribution != "MacOSX" }
    #- { role: xfce, when: ansible_distribution != "MacOSX" }
    #- { role: i3-gaps, when: ansible_distribution != "MacOSX" }
    #- { role: kubectl, ignore_errors: yes, become: true }
    ## - insync
    ##- google-cloud
#- hosts: rug
  #roles:
    #- minikube
    #- sbt
